# Solar GHI Prediction Backend — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# HTTP client (used by weather_service.py to call Open-Meteo)
httpx==0.27.2

# ML / numeric
xgboost==2.1.3
scikit-learn==1.5.2
numpy==1.26.4
joblib==1.4.2

# (No SHAP or frontend deps in this skeleton)
